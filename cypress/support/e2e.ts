// Importar comandos personalizados y plugins de accesibilidad/tecla TAB
import 'cypress-axe';
import 'cypress-plugin-tab';
import './commands';

// Configuración global para pruebas e2e
beforeEach(() => {
  // Acciones antes de cada prueba si es necesario
});

afterEach(() => {
  // Acciones después de cada prueba si es necesario
});
