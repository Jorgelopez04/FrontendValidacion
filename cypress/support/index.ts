// cypress/support/index.ts
// Importar comandos de componentes
import './component';
import './commands';

// Configuración global para pruebas de componentes
beforeEach(() => {
  // Acciones antes de cada prueba de componente si es necesario
});

afterEach(() => {
  // Acciones después de cada prueba de componente si es necesario
});
